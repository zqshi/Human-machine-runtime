{
  "name": "claude-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HMR Claude Agent SDK worker - 独立容器内执行单次 Agent 任务,通过 stdout NDJSON 上报进度",
  "main": "worker.js",
  "scripts": {
    "build": "tsc",
    "start": "node worker.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
