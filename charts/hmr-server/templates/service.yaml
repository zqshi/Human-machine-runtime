apiVersion: v1
kind: Service
metadata:
  name: {{ include "hmr-server.fullname" . }}
  labels:
    {{- include "hmr-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hmr-server.selectorLabels" . | nindent 4 }}
