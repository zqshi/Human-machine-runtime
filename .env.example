# DCF Light Bot — 环境变量说明
#
# 后端配置: server/.env.example
# Helm 生产配置: deploy/helm/dcf-light-bot/values-prod.yaml
# K8s 直部署模板: deploy/k8s/base/configmap.yaml + secret.example.yaml
#
# 使用方式:
#   cd server && cp .env.example .env
#   docker-compose up -d postgres conduit
#   npm run dev
