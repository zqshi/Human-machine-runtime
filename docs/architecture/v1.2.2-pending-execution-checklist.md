# v1.2.2 待办执行清单（新会话接手）

> 状态：`[PLANNED]` 待执行
> 来源：2026-06-26 四维审计 + 5 项修复（T38-T40）后的剩余阻断项
> 关联：`docs/versions/v1.2.2-current.md` 待办表（T42-T47）、记忆 [[audit-2026-06-26-fixes]]
> 用途：新会话按 §10.1 读 current.md 后，照此清单逐项执行。每项给：前置环境 / 执行步骤 / 验证信号 / 排查方向 / 完成判据

## 执行前准备（新会话必读）

1. **会话启动三步（§10）**：读 `docs/versions/v1.2.2-current.md` → 定位 T42-T47 状态 → 确认依赖
2. **环境自检**：
   ```bash
   curl -s http://localhost:3002/health        # server 存活
   docker exec -i human-machine-runtime-postgres-1 sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tA -c "select count(*) from tool_definitions where enabled"'
   # 应返回 1（T39 seed 的 check_system_health，工具闭环可触发）
   ```
3. **认证**：所有 dispatch/tool-invoke 端点需 token。登录拿：
   ```bash
   TOKEN=$(curl -s -X POST http://localhost:3002/api/auth/login -H "Content-Type: application/json" \
     -d '{"username":"tenant_admin","password":"tenant123"}' | jq -r .token)
   ```
4. **已完成进展（2026-06-26 会话，8 commit 已落 main，git log 可查）**：T38 externalTools 接线 / T39 tool_definitions seed / T40 openclaw 分页+addFragment 拆分+/metrics auth（commit a2abc6e→7a333d3，均已提交）；T42 tool-loop E2E 实测通过（commit 42dfa2a）；可观测性缺陷修复 /agent/status 端点（commit 1c26fd0，实测验证）；§14 门禁关键项检测通过。**不要重做**。

---

## ~~T42：tool-loop 主链路 E2E 实测~~（✅done 2026-06-26，commit 42dfa2a；P0，无需真 Anthropic key）

**目标**：验证实例任务工具闭环真跑通——glm-4-flash 真返回 tool_calls → registry.invoke 真执行 seed 工具 → 结论回填。tsc/vitest 测不出，必须真请求暴露。

**前置**：server(3002) + LiteLLM(4000, 已配 glm-4-flash key) + postgres(5435) 运行中；tool_definitions 有 seed 数据（T39）。

### 执行步骤

```bash
# 1. 登录拿 token（tenant_admin/tenant123）
TOKEN=$(curl -s -X POST http://localhost:3002/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"tenant_admin","password":"tenant123"}' | jq -r .token)

# 2. 发 dispatch 任务，明确指定 framework=tool-loop，prompt 引导 LLM 调 check_system_health 工具
TASK_ID=$(curl -s -X POST http://localhost:3002/api/openclaw/agent/dispatch \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "e2e-tool-loop-test",
    "description": "检查系统健康状态",
    "input": {
      "prompt": "请调用 check_system_health 工具检查系统是否正常，然后总结结果",
      "instanceId": "inst_showcase_06"
    },
    "framework": "tool-loop"
  }' | jq -r .taskId)
echo "taskId: $TASK_ID"

# 3. 轮询任务状态（tool-loop 异步执行）
sleep 3
curl -s http://localhost:3002/api/openclaw/agent/status/$TASK_ID -H "Authorization: Bearer $TOKEN" | jq

# 4. 查 tool_call_logs 确认工具真被调用（T39 seed 工具 definitionId=tdef_seed_health）
docker exec -i human-machine-runtime-postgres-1 sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "select id, definition_id, status, duration_ms, called_at from tool_call_logs order by called_at desc limit 5"'
```

### 验证信号（全过 = 通过）

- [ ] dispatch 返回 `taskId`（前缀 `tloop_`，证明 tool-loop adapter 接主链路）
- [ ] 任务状态最终 `completed`（非 failed/dispatched 卡住）
- [ ] `tool_call_logs` 表有新记录，`definition_id = tdef_seed_health`，`status = success`
- [ ] 任务 output 含 /health 返回的 JSON（`status:"ok","service":"hmr-server"`）

### 排查方向

- **dispatch 503**：`agentCore` 未注入 bootstrap，查 `server/src/app/bootstrap.ts` 注册段
- **taskId 非 tloop_ 前缀**：selectBestAdapter 没选 tool-loop，查 `adapter-registry.ts:59-65` 优先级
- **任务 failed "no tools"**：tool_definitions 表空（T39 seed 未跑），重跑 `npm run db:seed`
- **任务 completed 但无 tool_call_logs**：LLM 没返回 tool_calls（prompt 引导不够 / LiteLLM key 失效），查 LiteLLM 日志 `docker logs <litellm容器>`
- **tool_calls 有但 invoke 失败**：registry.invoke 闭环断，查 `tool-loop-executor.ts:143` + `tool-registry-service.ts:107`

### 完成判据

E2E 闭环跑通 + 截图/输出存档 → current.md T42 标 done。

---

## T43：worker externalTools 容器 E2E（P0，需真 Anthropic key）

**目标**：验证 T38 接线的 externalTools 在真 worker 容器里生效——boundTools 解析成 SDK custom tool → handler 回调 server /tool-invoke → 审批/凭证/计费/日志收口。

**前置**：真 `ANTHROPIC_API_KEY`（worker 走 claude-agent-sdk，非国产模型路径）；`CLAUDE_WORKER_E2E=1`。

### 执行步骤

```bash
# 1. 配真 key（.env，不进 git）
# ANTHROPIC_API_KEY=sk-ant-...   # 真 key
# CLAUDE_WORKER_E2E=1            # 开启集成测试

# 2. 重建 worker 镜像（worker.ts 改过需重建，T29 invokeToolWithServer+createSdkMcpServer 注入）
cd infra/claude-worker && docker build -t claude-worker:latest . && cd ../..

# 3. 跑集成测试（CLAUDE_WORKER_E2E=1 才不 skip）
cd server && CLAUDE_WORKER_E2E=1 npx vitest run --config vitest.integration.config.ts \
  src/contexts/agent-core/sandbox/claude-agent-sdk-adapter.integration.test.ts

# 4. 真发一个 boundTools 含 tdef_seed_health 的实例任务，验证 worker 回调 /tool-invoke
#    （需先建 agent_definition 绑定 tdef_seed_health，或用已有实例 + boundTools）
TOKEN=$(curl -s -X POST http://localhost:3002/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"tenant_admin","password":"tenant123"}' | jq -r .token)
curl -s -X POST http://localhost:3002/api/openclaw/agent/dispatch \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"name":"worker-e2e","description":"worker externalTools e2e","input":{"prompt":"检查系统健康","instanceId":"<绑定了tdef_seed_health的实例>"},"framework":"claude-agent-sdk"}'
```

### 验证信号

- [ ] 集成测试 4 项不再 skip（`CLAUDE_WORKER_E2E=1` 生效），全过
- [ ] worker 容器日志含 `invokeToolWithServer` 调用 + `createSdkMcpServer` 注入
- [ ] server `/api/internal/tool-invoke` 被回调（access log 或 tool_call_logs 新增 tdef_seed_health 记录）
- [ ] canUseTool 审批钩子触发（若 enforce 开）

### 排查方向

- **集成测试仍 skip**：`CLAUDE_WORKER_E2E` 未传入，查 `claude-agent-sdk-adapter.integration.test.ts:23`
- **worker 找不到镜像**：`docker images | grep claude-worker`，未重建则 `infra/claude-worker/Dockerfile` 重 build
- **worker 不回调 server**：externalTools 为空（T38 接线在 adapter:161 未生效），查 `claude-agent-sdk-adapter.ts` opts 是否含 externalTools + boundTools 是否真绑定了 tdef_seed_health
- **/tool-invoke 401**：`X-Internal-Secret` 不匹配，查 `CLAUDE_INTERNAL_TOOL_SECRET` worker 容器注入

### 完成判据

容器内 worker 真回调 server /tool-invoke 成功 + externalTools 注入日志存档 → T43 done。

---

## T44：上线前 DB 残留数据清理 + §14 版本门禁（P1，上线前最后一步）

**目标**：清实测残留数据，跑 §14 12 必修项全量审计，确保归档前全过。

### 执行步骤

```bash
# 1. 清实测残留（保留 seed 数据：tsrc_seed_demo/tdef_seed_health/4个seed账号/service_plans）
docker exec -i human-machine-runtime-postgres-1 sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB"' <<'SQL'
-- 清实测产生的调用/审计/日志（保留 schema 与 seed）
TRUNCATE tool_call_logs RESTART IDENTITY;
TRUNCATE audit_logs RESTART IDENTITY;
TRUNCATE billing_events RESTART IDENTITY;
-- 清非 seed 的 users（保留 admin/tenant_admin/ops/auditor + test1-5）
DELETE FROM users WHERE username NOT IN ('admin','tenant_admin','ops','auditor','test1','test2','test3','test4','test5');
-- 清实测 instances（保留 seed inst_showcase_06/07/08）
DELETE FROM instances WHERE id NOT LIKE 'inst_showcase_%';
SQL

# 2. 确认清理后状态
docker exec -i human-machine-runtime-postgres-1 sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tA -c "select (select count(*) from tool_definitions where enabled) as tools, (select count(*) from users) as users, (select count(*) from audit_logs) as audits"'
# 期望: tools=1, users=9, audits=0

# 3. §14 版本门禁 12 必修项（见 CLAUDE.md §14.1）
#    死代码/依赖卫生/配置一致性/文档对齐/架构合规/循环依赖/文件行数/仓库卫生
#    逐项跑检测脚本（§14.1 表格有方法列）
```

### 验证信号

- [ ] tool_call_logs / audit_logs / billing_events = 0
- [ ] users = 9（4 seed + 5 test），instances = 3（showcase）
- [ ] tool_definitions(enabled) = 1（保留 seed）
- [ ] §14 12 必修项全过（记录到 snapshot 的 `## 质量检测` 小节）

### 完成判据

数据清理 + §14 门禁全过 → T44 done → 可执行版本归档（§11.2）。

---

## T45：P1-6b tool-management-service 委托拆分（P1，低风险先做）

**目标**：639 行 service 拆 4 子 service，public 接口不变保护 17 测试。方案见 `v1.2.2-refactor-proposals.md` P1-6b 节。

### 执行步骤

1. 新建 4 文件：`tool-source-service.ts`（Source+Sync+Introspect+testConnection）/ `tool-definition-service.ts` / `tool-binding-service.ts` / `tool-execution-service.ts`（executeTool + resolveCredential）
2. `resolveCredential` 提取共享（ToolExecutionService 持有，其他注入或提取 helper）
3. `ToolManagementService` 改委托层：public 方法保留签名，内部 `this.sourceService.xxx()`
4. `bootstrap.ts` 不变（仍 new ToolManagementService，内部自组装）
5. 跑 `npx vitest run src/contexts/tool-management/` 确认 17 测全过
6. 跑 tsc + eslint 全量

### 验证信号

- [ ] 主文件 < 200 行，各子 service < 300 行
- [ ] 17 测试全过（接口不变）
- [ ] tsc 0 + eslint 0

### 完成判据

拆分完成 + 测试全过 → T45 done。

---

## T46：P1-5 presentation→infrastructure 重构（P1，3-5 天，分批）

**目标**：20+ 处直调改 application use-case hook。方案见 `v1.2.2-refactor-proposals.md` P1-5 节。分 3 批，每批独立可提交。

### 执行步骤（每批）

1. 新建 `application/use-cases/<feature>.ts` hook（封装 fetch + state + 错误 + 业务规则）
2. 改对应 presentation 文件 import（infrastructure → use-case hook）
3. 跑 client typecheck + vitest + eslint
4. 浏览器手测该功能

**批次**：
- 批次 1：McpDatabaseFlow / AgentCreateFlow / Credential*（核心创建流程）
- 批次 2：MarketplacePage / AgentAnalyticsPage
- 批次 3：admin 子组件（ToolApprovals/FeatureFlag/RuntimeTemplates）

### 完成判据

3 批全完成 + 全量门禁绿 → T46 done。

---

## T47：P1-7 跨聚合解耦（P1，2-3 天）

**目标**：studio-service / agent-definition-service / approval-gate 不直接引其他 context service。方案见 `v1.2.2-refactor-proposals.md` P1-7 节。

### 执行步骤

1. agent-core domain 新增 port：`ISkillPort` / `IAuditPort` / `ISystemConfigPort`
2. bootstrap 装配端口适配（仿 `assembly-provider.ts` 的 `adaptBoundTools` 模式）
3. 3 个 application service 改依赖 port 接口
4. 跑 tsc + vitest + eslint

### 完成判据

3 处跨聚合引用消除 + 全量门禁绿 → T47 done。

---

## 总执行顺序

```
~~T42 (主链路E2E,无需真key)~~  ✅done 2026-06-26(commit 42dfa2a)+可观测性修复(commit 1c26fd0)
  └→ T43 (worker容器E2E,代码就绪[T28/T29/T38],需真Anthropic key,硬阻断)
       └→ T44 (数据清理等T43 + §14门禁关键项已检测) → 上线
T45 (架构债,低风险)        ← 与上线正交,可并行择机
T46 (架构债,3-5天)         ← 下一版本
T47 (架构债,2-3天)         ← 下一版本
```

计费 T4-T11（v1.2.2 主题）排在上线之后（免费先上线）。
