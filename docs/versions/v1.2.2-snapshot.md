# v1.2.2 Snapshot — 投产收尾 + 架构债清零 + cockpit 命名中性化

> **归档时间**:2026-06-28。本版本从 current 归档(snapshot 只读,§11.3)。
> **归档原因**:v1.2.2 计费主题(T4-T11)未完成即归档——用户决策接受(计费转 v2.0 backlog,见遗留),为激活 v2.0 架构升级让出 current 唯一性(§11.1)。

## 目标

把 `tenant-management`(套餐)+ `quota-management`(配额)+ `analytics`(用量统计)+ `billing`(计费骨架)打通成可运营计费链路,支撑 Free/Pro/Team 三级定价。**本目标未达成**——计费 T4-T11 全 pending,本版本实际完成的是投产收尾(T12-T59)+ 架构债清零(T45-T47)+ cockpit 命名中性化(T60)。

## 交付摘要

本版本虽未完成计费主题,但完成了大量投产收尾与架构债清理,代码基座质量显著提升:

- **投产收尾(T12-T59)**:tool-management 凭证链路修复(T12)/ studio 实装去假桩(T13)/ chat guardrail 后端兜底(T15)/ 灰区实测清单(T16/T22)/ 多轮对话记忆修复(T49)/ ToolLoopExecutor 真执行链路(T32-T33,不被模型绑定)/ OpenSandbox 容器隔离沙箱(T53)/ sandbox 持久化(T55)/ Matrix bot 对话闭环(T57)/ cockpit 列表全量返回修复(T58)/ ChatService 用量入账(T59)
- **架构债清零(T45-T47,§12 信号 P1 全清)**:tool-management-service 委托拆分(T45)/ presentation→infrastructure 直连重构 13/13(T46)/ 跨聚合解耦端口接口 T47(8/8 port 全接线)
- **cockpit 命名中性化(T60)**:openclaw 自有子系统全栈改名 cockpit(DB+后端+前端+文档,250 文件,数据零丢失)
- **四维审计**:门禁全绿但揭示 §7.2.1 硬违规门禁测不出(grep repo.list 复核实际默认值),已转 T58/T59 修复

## 决策表

| 决策 | 选择 | 理由 |
|---|---|---|
| 实例任务执行不被模型绑定 | ToolLoopAdapter + 国产模型经 LiteLLM 多轮工具循环 | T32 用户决策"换厂商不被绑定+全链路真实";claude-agent-sdk worker 路径 A 强模型死结未解(T43) |
| 沙箱隔离 | OpenSandbox 容器隔离(docker),生产可升 Firecracker/Kata | 投产 P0 安全:LLM 生成代码独立容器,数据不出企业 |
| cockpit 改名彻底连 DB 一起改 | 幂等 RENAME migration | 用户决策;消除 openclaw 品牌名认知误导 |
| v1.2.2 归档(计费未完成) | 接受,计费转 v2.0 backlog | 为激活 v2.0 架构升级让出 current 唯一性;计费依赖固化配额(v2.0 manifest)才不漂移 |
| Matrix 不做 IM 绑定 | IM opt-in,不强制绑账号/房间 | 用户约束;processTextMessage 内置分流天然符合 |

## 遗留

> **本版本主题未完成项,转入 v2.0 backlog 处置。**

- **计费 T4-T11(本版本主题核心,全 pending)**:T4(quota↔analytics 贯通)/ T6(用量报表 API)/ T7(前端用量报表)/ T8(用量异常告警)/ T10(billing 写侧补全 consume/deduct/reserve)/ T11(日终对账)。**启动前置**:用户确认商业化时机 + 灰区实测(Matrix E2E/cockpit 浏览器实测,见 v2.0 current)。**归属**:转 v2.0 backlog 计费专项(v2.0 manifest 落地后计费基于固化配额,见 v2.0 设计文档 §10.3)
- **Matrix 端到端实测(用户侧)**:T57 代码 done,Conduit 起+注册 @hmr-bot 拿 token+前端 IM 对话验证 bot 回复/多轮/人↔人不介入。清单 `docs/architecture/matrix-bot-e2e-checklist.md`
- **cockpit 浏览器实测(backlog D10)**:管理后台新建 Agent→对话接线(T27)、共享 Agent 对话(T20b)前端 activeInstanceId 真设非 null + persona 真注入 + 真响应待浏览器 Console 实测
- **多轮记忆内存态→DB(T57 遗留)**:MatrixConversationStore 重启丢,升级 DB matrix_conversation_history 表
- **sessionId resume bug(T43 遗留)**:路径 A(claude-agent-sdk)未启用不触发,启用前修
- **/evaluation/scorecards 路径冲突**:evaluation.ts:32 与 bootstrap.ts:270 都注册 GET,既有 bug
- **agent-executor.ts 824 行**(§14-10):超 800 评估线未超 1000 红线,拆分待评估
- **§14-9 type-only 假环**:adapter-registry↔agent-runtime-adapter / recommendation-engine↔recommendation-prompt,编译擦除运行时单向无环,下版本清理类型归属

## 约束(对后续版本)

1. **商业模式数据流不可绕开 analytics**:计费事件必须落 analytics,不可只写 quota
2. **billing 写侧必须有对账**:不允许只累加 event 不核对,否则长期漂移不可发现
3. **计费基于固化配额才不漂移**:v2.0 manifest 落地是计费可信前置(v2.0 设计文档 §10.3)
4. **门禁测不出 §7.2.1 硬违规**:tsc/vitest 测不出 list 端点全量返回/缺列,须 grep repo.list + migration 复核(memory audit-2026-06-27-fixes)

## 质量检测(§14)

T44(2026-06-26)执行过 §14 门禁 12 必修项:8 通过 + 4 发现处置(死代码清理 27 删/§14-6 memory 索引偏差记债/§14-9 type-only 假环记债/§14-1/2 budget-guard 预留记债)。门禁基线:server tsc 0 + vitest 1751(3 skipped)+ eslint 0 / client tsc 0 + vitest 819 + eslint 0。T60(2026-06-27)cockpit 改名后门禁复测全绿。

---

> 详细任务记录见 git history(T12-T60)与本文件 git 历史版本(current 阶段完整记录)。接手待办见 `docs/architecture/pending-handoff-checklist.md`。
