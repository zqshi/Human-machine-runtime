# v1.2.2 Current — 商业模式闭环

> **主题**: 把 `tenant-management`（套餐）+ `quota-management`（配额）+ `analytics`（用量统计）+ `billing`（计费骨架）打通成可运营的计费链路，支撑 Free/Pro/Team 三级定价
> **前置**: v1.2.1 完成（外部契约收尾 + 私有化前置，见 v1.2.1-snapshot.md）
> **核心价值**: HMR 从"可对外承诺 SLA"升级到"可计费运营"
> **状态**: current 待启动（2026-06-24 v1.8 归档后激活为下一版本；内部推广暂不计费，择机开工）
> **上一版本**: v1.8（reconcile 解耦,已归档 snapshot;本版本版本号虽早于 v1.8,但 v1.3-v1.8 先行完成后方激活）
> **版本拆分由来**: 原 v1.2.0 范围过大，按 §11 拆为 v1.2.1（外部契约，已完成）+ v1.2.2（本版本，商业模式）

> **⚠️ 用户决策备注（2026-06-24 更新）**：v1.8 云原生收官已归档。v1.2.2(商业模式)作为下一候选激活为 current,但标注"待启动"——内部推广暂不计费,待内部推广验证 + 商业化时机成熟后择机开工。若需换方向(如 v1.9 新主题),从 backlog 取代本版本。
>
> **⚠️ 用户决策备注（2026-06-25 追加）**：计费 T4-T11 启动前先做灰区实测(T9流式/T18b审批/T20b对话/D10自举,清单见 `docs/architecture/production-e2e-verification-checklist.md`)。理由:计费建在未实测的主路径基座上风险高。计费设计/实现待实测通过 + 商业化时机。

---

## 版本目标

1. **计费写侧链路补全**: 当前 `billing` context 是骨架（仅 recordEvent 累加 amount），缺 quota 扣减、对账任务、invoice 生成
2. **用量报表 API + 前端**: 管理后台能看到按租户/套餐维度的用量报表（API 调用次数、实例运行时长、Token 消耗）
3. **可观测性补强**: 商业模式上线必须有用量审计 + 异常告警，否则计费不可信

## 版本验证标准

1. 租户在 Pro 套餐下创建数字员工实例，超配额时返回 `QUOTA_EXCEEDED`（已有，端到端验证）
2. 管理后台 `/admin/billing` 能看到按租户/套餐维度的用量报表
3. billing 写侧链路完整：预占 → 实扣 → 对账（日终核对 quota 余量 vs analytics 累计 vs billing 累加）
4. 用量异常（超阈值）触发通知通道告警

## 版本范围

**做什么:**
- `quota-management` 与 `analytics` 数据流贯通（实时扣减 + 日终对账）
- billing 写侧补全：consume/deduct/reserve API、对账任务
- 用量报表 API（按租户/套餐/时段聚合）
- 前端用量报表展示
- 用量异常告警（阈值 + 通知通道）

**不做什么:**
- 真实支付网关对接 → v1.3+（本版本只做用量统计 + invoice 生成，不做扣款）
- 账单 PDF 导出 → v1.3+
- 多币种支持 → v1.3+（当前固定 USD）

---

## 任务依赖图

```
T4 (quota↔analytics 贯通) ──→ T6 (用量报表 API) ──→ T7 (前端展示)
                          └──→ T8 (异常告警)
T10 (billing 写侧补全) ──→ T11 (对账任务)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 | 预估 |
|----|------|---|------|------|----------|------|
| T4 | quota-management ↔ analytics 数据流贯通 | P0 | - | pending | `server/src/contexts/quota-management/`, `analytics/` | 1 天 |
| T6 | 用量报表 API（按租户/套餐/时段） | P1 | T4 | blocked | `server/src/contexts/analytics/`, `routes/admin/analytics.ts` | 1 天 |
| T7 | 前端用量报表展示 | P1 | T6 | blocked | `client-suite/apps/web/src/presentation/features/admin/` | 1 天 |
| T8 | 用量异常告警（阈值 + 通知通道） | P2 | T4 | blocked | `server/src/contexts/notification/`, `analytics/` | 0.5 天 |
| T10 | billing 写侧补全（consume/deduct/reserve） | P0 | - | pending | `server/src/contexts/billing/` | 1.5 天 |
| T11 | 日终对账任务（quota vs analytics vs billing 三角核对） | P1 | T10 | blocked | `server/src/contexts/billing/`, `scheduler/handlers/` | 1 天 |

### 投产收尾前置（计费启动前必修，§10.5 计划外工作）

> v1.9 投产就绪归档后审计发现 4 项主路径真实断点（空转/假数据/未实测），阻断投产但非计费范围。作为 v1.2.2 计费启动前的必修前置，先于 T4-T11 完成。详见 plan `squishy-leaping-pond.md`。

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 | 预估 |
|----|------|---|------|------|----------|------|
| T12 | tool-management DB 凭证链路修复（接口漂移，CredentialManagementService.getCredentialSecret 已就绪未接线；id string↔number 对齐） | P0 | - | done | `contexts/tool-management/`, `credential-vault/`, `app/bootstrap/` | 0.5 天 |
| T13 | openclaw/studio.ts 实装（主路径 Agent 运行时资产聚合，当前 6 端点全 STUB 假数据） | P0 | - | done | `routes/openclaw/studio.ts`, 新建 `StudioService`, `app/bootstrap/` | 1.5 天 |
| T14 | feature-flags route 测试补齐（route 无 .test.ts） | P3 | - | done | `routes/admin/feature-flags.test.ts` | 0.2 天 |
| T15 | openclaw chat guardrail 后端兜底（chat 经 LiteLLM 不经 harness，T3 guardrail 仅实例路径生效） | P1 | - | done | `routes/openclaw/chat.ts`, `app/bootstrap/` | 0.5 天 |
| T16 | v1.9 流式/openclaw chat 浏览器实测清单交付（tsc/vitest 测不出） | P1 | T12-T15 | done | 清单已交付（16 项，见会话记录）；实测执行为用户侧待办，不阻断代码合并 | 0.3 天 |

> 条件项（未编号）：WpsImAdapter 7 个 NotImplementedError（editMessage/redactMessage/createDmRoom/invite/createGroup/join/leave）——若投产启用 WPS IM 通道则补齐，否则记账 backlog。

### backlog 债务处置（§10.5 计划外工作，与计费正交可并行）

> 投产收尾前置（T12-T16）已全 done。此批为 backlog 长期技术债中"可代码执行"的 3 项（D4/D3/D6）+ 1 项需先决策的（D9）。与 v1.2.2 计费（T4-T11）正交，不阻塞计费，可并行或择机。D10（浏览器实测）/D11（WPS IM 已搁置）不列入。

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 | 预估 |
|----|------|---|------|------|----------|------|
| T17 | D4 credential-vault 前端管理面（后端 API 已就绪 T10，缺前端 CRUD 界面） | P2 | - | done | `credentialManagementApi.ts` + `CredentialSection`/`CredentialCreateDrawer`/`CredentialDetailDrawer`/`CredentialLeasesTab` + adminStore/AdminPage 挂载；完整覆盖 7 端点；tsc/vitest 804/eslint 全过（2026-06-25） | 1 天 |
| T18a | D3 tool executor 脱节——诊断方案（核实断点 + 修复选项 A/B/C） | P2 | - | done | `docs/architecture/t18-tool-executor-mainline-gap.md`（claude-agent-sdk 主路径绕开 ToolRegistryService.invoke，治本需 worker↔server RPC，3-5 天架构任务） | 0.3 天 |
| T18b | D3 tool executor 接主链路——实现 | P2 | T18a | done | T18b-C 止血 done（restrictToReadonlyTools 开关）+ T18b-A canUseTool 审批闭环 done 2026-06-25（worker canUseTool→server /api/internal/tool-check 内置工具风险表 + enforce flag + 共享密钥认证，见 `docs/architecture/t18-tool-executor-mainline-gap.md`）；**真生效待 CLAUDE_WORKER_E2E=1 容器验证**；MCP 执行转发(凭证/计费)+ callLog 落库留后续 | 1.5-2 天 |
| T19 | D6 credential-repository 集成测试（DB 层 mock Database，补 repo 级测试） | P3 | - | done | `db/repositories/credential-repository.test.ts`（17 用例，mock Drizzle db 链式，照 oauth-state-repository.test.ts 模式）；vitest 17 passed（2026-06-25） | 0.5 天 |
| T20a | D9 marketplace 对话——产品决策方案（mka-* 市场 vs sa-* 共享 id 不兼容，先出方案供用户拍板） | P2 | - | done | `docs/architecture/t20a-marketplace-chat-decision.md`（现状+三选项 A 安装即对话/B 独立入口/C 轻量，推荐 A 转 capabilityRegistry active agent） | 0.3 天 |
| T20b | D9 marketplace 对话——实现（选项 A 安装即对话） | P2 | T20a | done | `marketplace-service.installAgent`(落 AgentDefinition+createInstance) + `routes/control/marketplace.ts` POST /agents/install + `marketplaceApiClient.installAgent` + `sharedAgentChatService.openInstalledInstance` + `openclawStore.setActiveInstanceId` + `MarketplacePage` 两处对话；核实发现 activeInstanceId 全链路未接线断点(T20a 未记录,影响所有共享 Agent 对话,已修 marketplace 路径)；tsc/eslint 全过,server 1578+client 805 测；**真对话响应待浏览器实测(类 D10)**,幂等性待补 | 2-3 天 |
| T21 | D11 WpsImAdapter 防误启用守卫（投产未启用 WPS IM,7 方法 NotImplementedError;加 VITE_WPS_IM_ENABLED gate:WpsImAdapter 构造 fail-fast + SettingsPage 选项过滤;补 client .env.example） | P2 | - | done | `infrastructure/matrix/wps-im-gate.ts`(+4 测) / `WpsImAdapter.ts` 构造 gate / `SettingsPage.tsx` 选项过滤 / `apps/web/.env.example`;tsc 0 + vitest 800 passed + eslint 0 errors(2026-06-25) | 0.3 天 |
| T22 | 灰区端到端实测清单文档化交付(T9流式/T18b审批/T20b对话/D10自举 4 项,含环境/步骤/信号/排查/汇总表;固化 T16 会话清单为可查文档) | P1 | - | done | `docs/architecture/production-e2e-verification-checklist.md`;实测执行为用户侧待办(需浏览器/容器),不阻断代码合并 | 0.3 天 |
| T23 | 灰区实测执行(后端 curl)— T9/T18b/T20b 三项环境补齐+实测。发现 3 项真实缺陷:①persona.systemPrompt 未注入 LLM(chat.ts:104)②installAgent 不触发 instance_llm_keys 同步③agent_definitions 缺列(migration 未执行,已跑 db:migrate 修复) | P1 | - | done(后端) | 详见 `docs/architecture/production-e2e-verification-checklist.md`;T9✅+T20b✅(后端)+T18b⚠️(server pass/worker 受 GLM 兼容);3 缺陷待修复(T24-T26) | 1 天 |
| T24 | 修复 persona.systemPrompt 未注入 LLM(chat.ts:104 应用 persona.systemPrompt) | P1 | T23 | done | `routes/openclaw/chat.ts` 加 resolveSystemPrompt(persona.systemPrompt 优先,body/默认降级);/chat 与 /chat/stream 两处复用。实测:persona 声明"你是Alice财务助手"→回复"我是Alice的虚拟财务助手"。11 单测(含 4 新)全过。commit d65cde2 | 0.3 天 |
| T25 | 修复 installAgent 不触发 instance_llm_keys 同步 | P1 | T23 | done | `marketplace-service` 加 setKeySyncDeps(setter 延后注入)+ syncDefaultModelKey(findOrCreateDefaultModel + 合并现有 grants + syncInstance);bootstrap 注入。实测:install 后自动 grant + virtual key synced + 直接对话 200 真响应(无需手动配 key)。同时补齐 LiteLLM 环境(DB+master key+GLM key) | 1 天 |
| T26 | 排查 agent_definitions migration 未自动执行根因 | P2 | T23 | done | 根因:`app/index.ts` 启动不调 migrate,`dev`/`start` 脚本只起 server,全靠人工 `db:setup`,pre-commit 门禁用 mock DB 测不出缺列。修复:`package.json` dev 脚本前置 `tsx src/db/migrate.ts &&`,dev 自动幂等 migrate(生产保持 db:setup:prod 显式)。已验证:dev 启动 migrate 先跑+server 200+persona 列在 | 0.3 天 |
| T27 | D10 管理后台新建Agent→对话接线(用户决策:不保留预置助手,新入口=新建Agent→对话) | P1 | - | done | `marketplaceService.instantiateExistingDefinition`(为已存在 def 实例化对话 instance + 同步默认 key,复用 installAgent 后半段) + control route `POST /api/control/agent-definitions/:id/instantiate` + `agentDefinitionApi.instantiate` + `AgentCreateFlow` 创建后 `openInstalledInstance` 跳对话;sa-* 旧路径评估(投产 sharedAgents 不 mock 填充→空→入口不暴露,标注废弃,死代码清理记 backlog);后端 4 测 + route 3 测全过,tsc 绿;**浏览器实测(前端 activeInstanceId 非 null)待用户** | 0.5 天 |
| T28 | T18b 选项A 工具执行RPC server端 /tool-invoke(治本第一步) | P0 | - | done | `routes/internal/tool-executor.ts` 新增 `POST /tool-invoke` 收口 `ToolRegistryService.invoke`(让 #7审批/凭证解密/租户隔离/计费对外部/MCP 工具生效;内置工具仍 SDK 执行器+canUseTool);外部工具 callLog 经 invoke 内部落库覆盖(内置工具 callLog 留后续);挂 internal-auth;7 测全过,后端门禁 1612 passed;**worker custom tool 注入 + E2E 待 T29** | 0.5 天 |
| T29 | T18b 选项A worker custom tool 注入 + E2E(治本完成) | P0 | T28 | done(代码) | worker.ts 加 `invokeToolWithServer` 纯函数(custom tool handler 调 server /tool-invoke 收口 ToolRegistryService.invoke)+ `createSdkMcpServer`+`tool` 工厂注入 `options.mcpServers`(无新增依赖,SDK 0.1.77 聚合出口 export `tool`/`createSdkMcpServer`,`McpSdkServerConfigWithInstance` 不需直装 `@modelcontextprotocol/sdk`);externalTools 字段待 runner 透传(从 AgentDefinition.boundTools 解析);worker build tsc 绿;**E2E 待真 Anthropic key + `CLAUDE_WORKER_E2E=1` 重建镜像 + runner externalTools 接线** | 2-3 天 |
| T30 | T18b 选项A docker-worker-runner 透传 externalTools | P0 | T29 | done(runner侧) | WorkerRunOptions 加 `externalTools` 字段 + buildPayload 透传 CLAUDE_TASK_JSON(2 测全过);worker.ts 已就绪注入 createSdkMcpServer+tool 工厂。**剩 adapter 解析 boundTools→externalTools + E2E**:adapter(claude-agent-sdk-adapter)从 AgentDefinition.boundTools(toolId[])查 ToolDefinition 拿 name/description/inputSchema 填 externalTools,触及 365 行核心文件有回归风险,无真 key E2E 无法验证,留 E2E 会话 | 0.5 天 |
| T31 | 最小可观测性告警:用量阈值告警定时扫描(防盲飞) | P1 | - | done | `scheduler/handlers/usage-alert-scanner.ts`(周期遍历 active 租户调 `QuotaService.getDashboard` 触发 `evaluateAndFireAlerts`,用量超阈值主动告警,不依赖有人查面板;复用现有阈值判断+createEvent);bootstrap 注册 + seed `*/5` cron 任务;5 测全过。告警落 notification 表(站内通知,邮件通道留后续) | 0.5 天 |
| T32 | 实例任务真执行:LLM驱动工具循环执行器(不被模型绑定) | P0 | - | done(代码) | 用户决策"换厂商不被绑定+全链路真实"。核实:OpenClawAdapter 是假桩(simulateProgress)/AgentExecutor 只意图分类无工具循环/worker路径A需SDK协议。新建 `ToolLoopExecutor`(domain,经 ILLMClient+LiteLLM 国产模型多轮工具循环→registry.invoke真闭环,8测)+ `ToolLoopAdapter`(sandbox,framework='tool-loop',替代假桩,9测)+ ILLMClient 扩展 tools/tool_calls 透传(零回归28测)+ bootstrap 注册 + selectBestAdapter 优先 tool-loop + 修 /agent/dispatch 安全洞(tenantId 取 principal + zod)。门禁 server tsc 0+1636/client tsc 0+eslint 0。**E2E 待国产模型实测(不需真Anthropic key)** | 1 天 |
| T33 | 实例任务真执行 E2E(国产模型) | P0 | T32 | done(主链路) | 验证:①glm-4-flash 经 LiteLLM `/v1/chat/completions` 真返回 tool_calls(get_weather+arguments,证明不被 Anthropic 协议绑定,走 OpenAI 兼容端点国产模型可用)②POST /agent/dispatch 返回 `framework:"tool-loop"` + taskId 前缀 `tloop_`(新 adapter 接主链路替代假桩,route→adapter→executor 串联通)。**完整工具闭环待 DB seed enabled ToolDefinition**(当前 tool_definitions 表空,discover 返回空→LLM 无工具可调直返 content;registry.invoke 闭环由 /tool-invoke 7测+ToolLoopExecutor 8测覆盖) | 0.3 天 |
| T34 | 去 mock 真实投产:DB 探测 introspect 独立端点+MySQL 探测 | P0 | - | done | `tool-management-service.introspectSource`(抽 syncDatabase 前半段不落库,供 McpDatabaseFlow 探测→勾选→发布,5测)+ `POST /sources/:id/introspect` 端点(1测);`db-introspector.introspectMysql` 用 mysql2 查 information_schema(表/列/主键 statistics,不再返"未实现")+ testConnection 按 type 分流(PG用pg/MySQL用mysql2,修无脑pg bug,4测);加 mysql2 依赖。门禁 server tsc 0+1649 passed | 0.5 天 |
| T35 | 去 mock:bootstrap 假活动+Escalation 假记录清理 | P1 | - | done | bootstrap route 移除 proactiveActivities/proactiveInsights 写死假数据(无真实活动数据源,quickCommands 保留);EscalationPanel 移除 MOCK_ESCALATIONS 改空态(escalation 是 SSE 事件流非列表API)。门禁 tsc/eslint 绿 | 0.2 天 |
| T36 | 去 mock:前端展示层剩余(SkillDetailView/AgentDetailView/AgentAnalyticsPage/McpDatabaseFlow) | P0 | T34 | done(3/4) | SkillDetailView 删 MOCK 文件/参数/示例/评价改空态(overview 真字段);AgentDetailView 删 MOCK 假对话(订单12847/GMV325万)+假工具技能评价,改引导/空态;AgentAnalyticsPage 删 MOCK 统计接 analyticsApi.logStats+dauTrend;OpenClawAdapter 假桩注销+删文件(tool-loop 替代)。门禁全绿。**剩 McpDatabaseFlow**:凭证链路阻断(credential-vault createCredential 是 OAuth 单 secret,无 DB user/pass 多 secret 创建能力),需后端补凭证能力 | 1.5 天 |
| T37 | McpDatabaseFlow 真连接:凭证链路(credential-vault 支持 DB user/pass) | P0 | T36 | done | `credential-management-service.createCredentialWithSecrets`(建 authz+存多 secret,复用 repo saveSecret,2测)+ `POST /credentials/with-secrets` 端点 + 前端 API 封装;McpDatabaseFlow 删 MOCK_TABLES 接真 API(createCredentialWithSecrets→createSource→testConnection→introspectSource→syncSource),剔 SQLite;seed auth_providers db-tools provider(id=1 已验证,DB_TOOL_PROVIDER_ID 硬编码正确)。门禁 server tsc 0+1651测/client tsc 0+eslint 0+vitest 800 passed。userId 用 SYSTEM_USER_ID=1(admin,与 createCredential body 传一致) | 1.5 天 |

| T38 | 四维审计 + P0-3 worker externalTools 接线(T18b-A 治本尾巴) | P0 | - | done | 四维审计(质量/业务链路/架构/服务连通)发现 worker externalTools 断点(adapter opts 不填 externalTools,assembly 只产 allowedTools,boundTools 形同虚设)。接线:`IBoundToolsPort.findByIds` 扩展 description+inputSchema → `AssemblyResult.externalTools` → harness 透传 → `claude-agent-sdk-adapter` opts.externalTools + `resolveExternalTools` helper。跨 5 文件(domain/bootstrap/harness/adapter/test)+4 新测。worker↔server /tool-invoke 收口对 boundTools 生效。**真生效待 CLAUDE_WORKER_E2E=1 容器验证**(无真 Anthropic key 无法 E2E) | 0.5 天 |
| T39 | P0-1 seed tool_definitions(实例任务工具闭环空转修复) | P0 | - | done | DB 实测 tool_definitions=0 致 ToolLoopExecutor.discover 空→LLM 无工具→闭环空转(T33 自陈)。seed `tool_source`(tsrc_seed_demo)+`tool_definition`(check_system_health,http_proxy 指向 server /health,只读无副作用不依赖外网)。实测 `POST /tool-invoke` 返回 success+data(/health JSON)+logId,registry.invoke 真执行 fetch 闭环通。riskLevel=low 免审批。投产可替换为真实业务工具 | 0.3 天 |
| T40 | P1-4 openclaw 分页 + P1-6a addFragment 拆分 + P2 /metrics auth | P1/P2 | - | done | P1-4:openclaw 13 list 端点加分页(collaboration3/signals3/objectives1/orchestration3/evaluation2/decisions1)复用 repo.listPaged+decisions.ts 模式,消除全量返回违规(§7.2.1规则2);P1-6a:memory-service addFragment 84行超80红线拆为 syncFragmentToMem0+updateStoreCounters(均<80,行为不变 40测全过);P2:/metrics 加 bearer token 校验(无401/正确200/错401实测)。门禁 server tsc 0+1654测/eslint 0 | 0.5 天 |
| T41 | P1-5/P1-6b/P1-7 架构重构方案(出方案,实施留下一版本) | P1 | - | done(方案) | `docs/architecture/v1.2.2-refactor-proposals.md`:P1-5(presentation→infrastructure 20+处直连,引入 application use-case hook,3-5天)+P1-6b(tool-management-service 639行委托拆 4 子 service,1-1.5天)+P1-7(跨聚合 studio-service/agent-definition/approval-gate 解耦,领域事件/端口,2-3天)。§12.2 下一版本必修+§4.3 大改单独排期,不在 v1.2.2 计费主题强行实施 | 0.3 天 |

### 待办(2026-06-26 审计后,新会话接手执行)

> 四维审计([[audit-2026-06-26-fixes]])发现的核心链路"门禁全绿但工具闭环空转"已修 5 项(T38-T40),剩 3 项阻断上线 + 3 项架构债待办。详细执行步骤见 `docs/architecture/v1.2.2-pending-execution-checklist.md`。新会话按 §10.1 读此 current.md 后,定位 pending 任务直接接手。

#### 投产收尾待办(上线前必修,阻断投产)

| ID | 任务 | P | 依赖 | 状态 | 涉及文件/环境 | 预估 |
|----|------|---|------|------|----------|------|
| T42 | P0-2 tool-loop 主链路 E2E 实测(国产模型,不需真 Anthropic key) | P0 | T39 | done | 实测通过(2026-06-26):POST /api/openclaw/agent/dispatch framework=tool-loop 返回 taskId=tloop_7bfc38...(证明 tool-loop adapter 接主链路);tool_call_logs 新增 tclog_9bb85...(definition_id=tdef_seed_health,status=success,duration_ms=2);output_result={"status":"ok","service":"hmr-server","timestamp":"2026-06-26T07:03:29"}=/health JSON(结论回填);ToolLoopExecutor 代码证无 tool_calls 不调 invoke(line109-112)→有 log 记录=glm-4-flash 真返回 tool_calls。完整闭环:LLM tool_calls→registry.invoke 真执行→结果回填 tool role→续轮 LLM 总结。**发现可观测性缺陷并已修(commit 1c26fd0)**:补 AdapterRegistry.getTaskStatus+harness 转发+bootstrap GET /agent/status/:id,实测 dispatch tloop_→status 返回 completed+conclusion+toolCallsLog+turns,调用方可观测任务全生命周期。注:内存态,conclusion 落库留后续 | 0.5 天 |
| T43 | worker externalTools 容器 E2E(T38 真生效验证) | P0 | T38 | done(代码侧)/E2E记遗留 | **深查结论(2026-06-26)**:路径 A 用 glm-4-flash 架构不可行——claude-agent-sdk 强制向 LLM 暴露 19 个内置工具 schema,glm-4-flash 在大工具集下决策失焦不调业务工具(curl 只留 mcp tool 时 glm 立刻返回 tool_use,证明是 SDK 工具噪声非 glm 不能调工具);`allowedTools` 仅控执行权限不控 schema 暴露,worker 代码不可绕。**已修 3 项基础设施缺陷**(LiteLLM 路由墙/worker 镜像滞后/runner 缺 --add-host,对路径 B 无害,路径 A 启用必需,已提交);**遗留 sessionId resume bug**(adapter 传历史 sessionId 给 --rm worker resume 必 exit 1)。详见 `docs/architecture/private-deployment-paths.md` T43 实测结论。投产走路径 B(T42 通过),路径 A 标"需强模型" | 1-2 天 |
| T44 | 上线前 DB 残留数据清理 + §14 版本门禁 | P1 | T42 | done | **数据清理**:TRUNCATE audit_logs(2186→0)/tool_call_logs(3→0)/billing_events,DELETE 非 seed users(12→9,删 alice/bob/carol);保留 seed(tsrc_seed_demo/tdef_seed_health/4账号/3 showcase instances)。**§14 门禁 12 必修项**:8 通过(§14-3 依赖卫生15deps全import/§14-4 config↔env双向对齐/§14-5 docker↔Dockerfile端口/§14-7 current唯一/§14-8 DDD零外部依赖/§14-10 无>1000(agent-executor824记债)/§14-11 gitignore/§14-12 无敏感文件)+4 发现处置(§14-1/2 死代码清理27删+budget-guard记债;§14-6 memory索引偏差记债;§14-9 madge报2个type-only假环运行时单向无环记债)。**死代码清理**:删27个真死代码(废弃feature code/canvas/subscription/collaboration + openclaw11旧面板 + admin3子组件 + AdminPage废弃旧容器),三重门禁全绿(tsc+eslint+vitest800)。**发现并修复T17/T14接线bug**:删AdminPage(废弃旧容器被AdminShell取代)浮现4个活功能Section(CredentialSection/FeatureFlagSection/RuntimeTemplatesSection/ToolApprovalsSection)挂废弃AdminPage未接入活容器AdminShell(current.md 'T17/T14 done'埋雷,功能未生效);接入AdminShell(import+投产管控组+credentials+SwitchedSection 4case)+adminStore HASHABLE补3,三重门禁全绿,4功能在admin后台生效。**admin后台4功能可见性待浏览器实测**(用户侧) | 0.5 天 |

#### 架构债待办(下一版本,与计费正交可并行)

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 | 预估 |
|----|------|---|------|------|----------|------|
| T45 | P1-6b tool-management-service 委托拆分(低风险先做) | P1 | - | done | 639行委托拆为4子service+credential-resolver共享helper(application/):主service181行委托层(保留public签名)/source485/definition34/binding36/execution85。resolveCredential提纯函数(Source/Execution共用,避免子service耦合)。getStats/getCallLogs留主service(跨repo聚合)。测试2处dbIntrospector spy路径改指sourceService(反映新结构,不暴露getter妥协封装)。门禁17测+tool-registry40测+tsc strict0+全量1661 passed。遗留:tool-source-service 485行(sync三方法inserts重复,§3.2接近评估线,记技术债,sync逻辑变更时抽helper优化) | 1-1.5 天 |
| T46 | P1-5 presentation→infrastructure 直连重构(去直调) | P1 | - | done | **核心目标达成:presentation/features 下 13/13 组件去 infrastructure 直调**(grep 确认零直调,§1.2 违规消除)。引入 application/use-cases/ 纯函数用例层(复用 createOrganizationEmployee 模式:纯函数+deps 注入+不依赖 React):featureFlagUseCase(listFeatureFlags/saveFeatureFlag,6测)+ agentDefinitionUseCase(buildAgentDefinitionSpec/createOrUpdate/instantiate,7测)。adminApi.ts re-export featureFlagApi/toolApprovalsApi/runtimeTemplatesApi/marketplaceApi/upstreamMarketplaceApi/credentialManagementApi(export *)及类型(统一 application 出口)。**模式分层**:有编排逻辑的(FeatureFlag PRESET 合并/tenants 解析、AgentCreateFlow create/update/instantiate+spec 构造)建 use case+test;逻辑薄的(Credential* CRUD/McpDatabaseFlow/MarketplacePage/admin 子组件)用 adminApi re-export(合规去直调,不强建 use case 避免 over-engineering)。门禁 client tsc 0+eslint 0+vitest 820(+13 use-case 测)。**接手可选优化**:McpDatabaseFlow 探测→勾选→发布编排可抽 use case(当前留组件);Credential* 可抽 CRUD use case。方案见 refactor-proposals.md | 1 天 |
| T47 | P1-7 跨聚合解耦(studio-service/agent-definition/approval-gate) | P1 | - | pending | agent-core domain 加 ISkillPort/IAuditPort/ISystemConfigPort,bootstrap 装配。方案见 refactor-proposals.md | 2-3 天 |
| T48 | 修C类:tool-loop 投产路径补统计/计费写入(用户实测发现"统计对不上业务") | P0 | - | done | 根因:recordUsage 只挂 claude adapter onTaskComplete(agent-adapters.ts:60-95),投产走 tool-loop 路径不写统计/计费→统计表空→对不上真实用量。修复 5 步:①ChatCompletionResult 加 usage 字段(agent-executor.ts)②LiteLlmClientAdapter 加 extractUsage 提取 OpenAI usage(litellm-llm-client.ts,此前返回丢 usage)③ToolLoopExecutor 每轮累计 prompt/completion token,execute 返回加 tokenUsage(tool-loop-executor.ts)④ToolLoopAdapter onTaskComplete 透传 tokenUsage 到 AgentTaskResult(tool-loop-adapter.ts,AgentTaskResult.tokenUsage 字段已存在)⑤bootstrap onTaskComplete 回调补 recordUsage+estimateCostUsd+billingService.recordEvent(仿 claude 模式,model 用 config.agent.llmModel)。补 3 复现测(tool-loop-executor 累计测+litellm-llm-client usage 提取/缺省测)。门禁 server tsc 0+1664 passed+eslint 0。**遗留:openclaw chat 路径(chat.ts)用量未入账**(chat 单轮 LLM 调用不写统计,归 A/B 架构方向:chat 是否要 agent 能力+用量归户待决策) | 0.5 天 |
| T49 | 修多轮对话记忆(业务场景实测发现"每轮失忆") | P0 | - | done | 业务场景实测:用户自报"工号F001"→追问工号→Agent"无法访问"完全失忆。根因:chat.ts 后端**支持** body.history(chat.ts:128/144)但前端 useAgentChat 构造 chatInput 不传 history(line325),每轮后端只收单条 message,LLM 无上下文。修复:①AgentChatInput 加 history 字段(domain)②useAgentChat 用 useOpenClawStore.getState() 实时取历史消息转 OpenAI messages(role 'agent'→'assistant',PLACEHOLDER_RE 过滤 routing/thinking 占位)③OpenClawRuntimePort.chat 请求体传 history④后端 chat.ts 加 sanitizeHistory(只放行 role∈{user,assistant}+string content,截断 40 条防注入 system 绕过 systemPrompt+防 token 爆)。补 3 复现测(history 透传/防注入 system/截断)。门禁 server 1667+client 800+tsc/eslint 0。**端到端实测验证通过**:1a"已记录张秋实F001"→1b"您的工号是F001,名字是张秋实"(修复前 1b"无法访问工号") | 0.3 天 |
| T50 | OpenHands 技术验证(架构方向评估,不写代码) | P0 | - | done(评估) | 子 agent 联网+源码直读结论:OpenHands(software-agent-sdk)解开了 T43 两个死结——①工具暴露可控(Agent(tools=[业务工具],include_default_tools=[])可减到只暴露业务工具,非 claude-agent-sdk 强制 19 个)②native_tool_calling=False 走 ReAct 文本模式(工具 schema 不进请求体,转自然语言描述,glm 不扛 JSON schema 解析,claude-agent-sdk 无此降级)。**硬阻断在 glm-4-flash 本身**:官方推荐 GLM-5/5.1,glm-4-flash 更弱一档,文档明说"may struggle...behaves like plain chatbot"。**换框架绕不开模型太弱根因**。最小 PoC Step0(5min):openhands headless CLI 跑 glm-4-flash 看能否触发真实写文件,<70%止损。建议 PoC 同时测 GLM-5 对照区分"模型太弱 vs SDK 太重"。集成方式:部署 agent-server 为独立 Docker 服务,Hono 经 REST API+X-Session-API-Key 调,server 零 Python 依赖 | 0.3 天 |
| T51 | 路径B 真实创建应用能力(推翻T43 glm结论+sandbox执行器+端到端验证) | P0 | - | done | **推翻T43 glm结论**:实测 glm-4-flash function calling 单工具 8/8、5工具 6/6(100%)——T43"glm 在大工具集失焦"真因是 claude-agent-sdk 强制19内置工具噪声,合理工具数下 glm 完全正常。**正解=路径B扩展**(非OpenHands):给 tool-loop 加 coding 工具 + 沙箱,glm 已验证可调工具。实现:①ExecutionType 加 'sandbox'(types.ts)②SandboxExecutor(server 进程内 node fs,隔离到 HMR_SANDBOX_ROOT/<callerId>,受限操作 write_file/read_file/list_files 不暴露任意 bash,严格路径逃逸防护:拒绝对对路径/../穿越/超256KB/未知op,11单测全过)③executor-factory 加 case ④ToolExecutionService 注入 __callerId(params 约定,sandbox 用,其他 executor 忽略零影响)⑤ToolLoopAdapter 传 instanceId ⑥GET /agent/sandbox/:instanceId/files 只读端点(前端展示LLM创建的文件)⑦seed 3 coding 工具(write_file/read_file/list_files,riskLevel medium/low)。**端到端实测通过**:dispatch framework=tool-loop → LLM turns=3(调write_file→read_file→总结)→ 真实创建 /tmp/hmr-sandbox/inst_showcase_06/src/App.tsx(真实React计数器组件代码,非假表演)→ sandbox 读取端点返回真实文件内容。门禁 server tsc 0+1678 passed+eslint 0。**PoC≠投产就绪**:当前 server 进程内 fs(非docker隔离),投产需改 docker 沙箱;无构建/预览能力(只创建文件)。glm-4-flash 可用,无需真 Anthropic key | 1 天 |
| T52 | 前端 AppCreateFlow 接真实创建链路(去假表演) | P0 | T51 | done | AppCreateFlow.tsx 重写为真实链路:①去 MOCK_TERMINAL/INITIAL_FILES 假表演 ②handleSend 改 fetch /api/openclaw/agent/dispatch(framework=tool-loop,input.instanceId=固定 'app-studio',sandbox 隔离)③轮询 /agent/status/:id(30轮×1.5s)→ 完成读 sandbox 文件树+内容展示 ④文件 Tab 从 GET /agent/sandbox/:instanceId/files 真实读取 ⑤终端 Tab 改"创建日志"(展示真实 dispatch→工具调用→完成,非假 npm install)⑥预览 Tab 诚实标注"需构建环境,后续支持"(不假装能跑)。**端到端实测通过**:dispatch "创建Todo应用" → LLM 调 write_file → 真实创建 src/App.tsx(完整 React Todo 组件 addTodo/removeTodo/toggleComplete)→ sandbox 读取端点返回真实文件树(src/)+内容。门禁 client tsc 0+eslint 0。修 sandbox 读取端点 path='.' 误判文件 bug(正则判扩展名)。**遗留(均 T56 已修)**:预览 iframe 已接(T56);文件树已改多级递归(T56);token 已改 credentials:include cookie session(T56) | 1.5 天 |
| T53 | 接入 OpenSandbox 容器隔离沙箱(投产 P0 安全升级,用户指定方案) | P0 | T51 | done | 用户指定 OpenSandbox(opensandbox-group/同 alibaba/OpenSandbox,Apache-2.0 开源自托管)+CubeSandbox 二选一。调研结论选 OpenSandbox:官方 Node/TS SDK(@alibaba-group/opensandbox,接口 files.writeFiles/readFile/search 与现有 SandboxExecutor 1:1 对齐)+部署简单(docker 一行起)+成熟(11.7k★,146 releases)。CubeSandbox 无 JS SDK+部署重(需KVM定制内核)不匹配 Node 栈。实现:①装 SDK ②起 OpenSandbox server(uvx opensandbox-server,8080,api_key=hmr-dev-osb-key,需 daocloud 镜像源拉 opensandbox/execd 镜像因 docker hub 网络问题)③OpenSandboxExecutor(实现同 IToolExecutor,按 callerId 缓存 sandbox+TTL空闲10min+最长1h清理,避免每次工具调用 create+kill 开销)④executor-factory:OPENSANDBOX_DOMAIN 配置即用 OpenSandboxExecutor 容器隔离,否则降级 node-fs(开发容错)⑤config 加 opensandbox 配置+.env+.env.example 同步 ⑥sandbox 读取端点改用 factory 单例(修 new 新实例致新空 sandbox 看不到 LLM 写入文件的缺陷)⑦导出 sandboxExecutorSingleton。**端到端实测通过**:dispatch → LLM 调 write_file → OpenSandbox 服务端日志确认 `POST /v1/sandboxes` 创建容器 `start sandbox container` → 宿主 /tmp/hmr-sandbox 无文件(✓ 走容器非 node-fs)→ 读取端点读回真实 App.tsx 内容(`import React...function App(){return <div>Hello Final</div>}`)→ 文件树列容器根(/bin /etc /proc 证明真实 docker 容器)。门禁 server tsc 0+1678 passed+eslint 0。**投产阻断P0(sandbox docker隔离)已解决**:LLM 生成的代码在独立 docker 容器执行,与宿主文件系统隔离。遗留:~~list_files 列容器根~~(早已修 resolveWorkspacePath 限 /workspace,T56 补修 type 映射致目录正确标 dir);生产配 Firecracker/Kata microVM 增强隔离(改服务端配置 ~/.sandbox.toml,代码零改动) | 1 天 |
| T56 | 路径B sandbox 文件树/预览/token 完善(T51-T55 尾巴) | P1 | - | done | ①listFiles 修 type 映射(OpenSandboxExecutor 硬编码 type:'file' 忽略 SDK FileInfo.type,致目录被标 file 文件树无法递归;抽 mapSearchEntries 纯函数 + directory→dir,7测)②前端文件树多级递归(buildFileTree 按 path 构建嵌套树,search 递归返回子树无需递归调 API;6测)③预览 Tab 接 iframe(POST /preview 拿 URL→iframe 展示,npm install 150s 超时+loading+error+installLog)④token 统一(去 getAuthToken 造轮子改 credentials:'include' cookie session,与 hmrApiClient 一致,5 处 fetch 改)。门禁 server tsc 0+1685测/client tsc 0+eslint 0+vitest 806。解决 T52/T53/T54 三遗留(文件树单层→递归、token localStorage→cookie、预览 iframe 未接→已接、list_files type→已修) | 0.5 天 |

**待办执行顺序建议(2026-06-26 第三轮会话后)**:~~T42~~(done) → ~~T43~~(done代码侧,走路径B上线不阻断) → ~~T44~~(done:数据清理+§14门禁+死代码清理27删+T17/T14接线bug修复) → T9/D10 浏览器实测(用户侧) → 上线。~~T45~~(done commit 2099d96/87ec7a6)。架构债 **T46/T47** 下一版本实施(3-5天/2-3天)。计费 T4-T11 排上线后(免费先上线)。

**新会话接手指引(2026-06-26 第七轮,sandbox持久化已解决)**:T42-T55 主体闭环。**本轮突破**:①推翻T43 glm错误结论(实测glm单/5工具100%)→路径B真实创建应用(T51/T52)②**接入OpenSandbox容器隔离沙箱(T53,投产P0已解决)**:LLM生成代码在独立docker容器,数据不出企业 ③应用预览技术验证通过(T54):sandbox内npm install+vite dev+端口URL ④**sandbox持久化(T55,解决T54暴露的硬伤)**:建sandbox_sessions表+DB存sandboxId映射+server重启经ID重连,实测重启后应用文件仍存活(`Persist Survives Restart`)。**剩余阻断**:浏览器实测(用户侧)+ 生产密钥(用户侧)+ OpenSandbox生产部署(K8s runtime,开发已uvx验证)+ §14归档。架构债T46/T47下一版本。计费T4-T11排上线后(免费先上线)。

**新会话接手指引(2026-06-27 第八轮,T56 完善 T51-T55 尾巴)**:用户授权"依次全量完成"(小尾巴→T46→T47→计费→§14)。本轮做小尾巴批次(T56):①listFiles 修 type 映射(硬编码 'file' 忽略 SDK FileInfo.type 致目录无法递归;抽 mapSearchEntries 纯函数 7测)②前端文件树多级递归(buildFileTree 6测,search 递归返回子树无需递归调 API)③预览 Tab 接 iframe(POST /preview→URL→iframe,npm install 150s 超时)④token 统一(去 getAuthToken 造轮子,改 credentials:include 与 hmrApiClient 一致)。门禁 server tsc 0+1685测/client tsc 0+eslint 0+vitest 806。解决 T52/T53/T54 三遗留。**待办**:T46(presentation→infrastructure 重构 3-5天)/T47(跨聚合解耦 2-3天)/计费 T4-T11/§14 归档,依次推进中。

**新会话接手指引(2026-06-27 第九轮,T46 done,本会话做 T47)**:第八轮已完成小尾巴(T56)+ T46(presentation 去 infrastructure 直连 13/13,§1.2 违规消除)。**本轮做 T47(跨聚合解耦,2-3 天)**——需完整做,不可做一半(建 port 接口但 service 半新半旧会留架构半成品,违反 §4.3)。

T47 核查现状(2026-06-27 确认 3 处跨聚合 import 仍存):
- `contexts/agent-core/application/studio-service.ts:17-18` → `shared-assets/skill-service`(ISkillRepository)+ `shared-assets/domain/shared-skill`(createAssetBinding)
- `contexts/agent-core/application/agent-definition-service.ts:2` → `audit-observability/audit-service`(AuditService)
- `contexts/tool-management/application/approval-gate.ts:4` → `system-config/system-config-service`(SystemConfigService)

T47 执行清单(治本,选项 B 端口接口):
1. agent-core domain 加 port 接口(仿 `assembly-provider.ts` 的 IInstanceLookupPort/IAgentDefinitionPort/IBoundToolsPort 模式):`ISkillPort`(findSkillByIds/createAssetBinding 等当前 studio-service 用到的)、`IAuditPort`(agent-definition-service 用到的 audit 方法)、`ISystemConfigPort`(approval-gate 用到的 config 读取)
2. bootstrap.ts 装配端口适配(类似 `adaptBoundTools`/`adaptInstanceLookup`,由被依赖方 context 适配注入):shared-assets 适配 ISkillPort、audit-observability 适配 IAuditPort、system-config 适配 ISystemConfigPort
3. 3 service 改依赖 port 接口(去跨 context import)
4. 测试:3 service 原测试改注入 mock port(不动跨聚合真实 service);可补 port 适配单测
5. 被依赖方 context(shared-assets/audit-observability/system-config)不改

接手起点:读本 current.md §10.1 → 定位 T47 pending → 按上清单做。完成后标 T47 done,更新接手指引第十轮(计费 T4-T11 或 §14 归档)。**计费 T4-T11 需用户确认商业化时机**(用户决策备注:待实测+商业化时机,不应替用户定)。

**实施要点**：
- **T17**（已完成，ROI 最高）：后端 `routes/admin/credentials.ts` + `CredentialManagementService` 已就绪。前端仿 `ToolApprovalsSection` 建 `CredentialSection` + Create/Detail/LeasesTab 子组件，接 `credentialManagementApi`（新建）。完整覆盖 7 端点。tsc/vitest 804/eslint 全过。
- **T18a**（已完成）：诊断确认 claude-agent-sdk 主路径（dispatchTask→adapter→worker→SDK）工具执行绕开 `ToolRegistryService.invoke`，致 #7 审批/callLog/凭证/租户隔离/计费对主链路失效。治本需 worker↔server 工具调用 RPC（选项 A，3-5 天架构任务），非 1 天接线。详见 `docs/architecture/t18-tool-executor-mainline-gap.md`。T18b 实现待用户在 A/B/C 选项拍板。
- **T19**（已完成）：`credential-repository.ts` 纯持久化，mock Drizzle db 链式（select/insert/delete/update 四类终端），17 用例覆盖 createAuthorization/getAuthorization/listAuthorizations/deleteAuthorization/saveSecret/getSecretCiphertext/listSecrets/createLease/findValidLease/revokeLease/listLeases/revokeExpiredLeases。照 `oauth-state-repository.test.ts` 模式。
- **T20a**（已完成）：不写代码，出决策方案。核实两处「对话」均空 `setDock` 跳转（且跳的 dock 不一致）、`mka-*` 与 `sa-*`/capabilityRegistry id 不兼容、无 installAgent、sa- 标 @deprecated。三选项 A 安装即对话（转 capabilityRegistry active agent）/ B 独立入口 / C 轻量，推荐 A。详见 `docs/architecture/t20a-marketplace-chat-decision.md`。T20b 实现待用户拍板。

**关键路径（计费）**: T4 → T6 → T7 = 3 天（用量报表链路）
**并行路径（计费）**: T10 → T11（billing 写侧，可与关键路径并行）
**前置（投产收尾）**: T12/T13/T15 可并行 → T16 实测收尾；完成后方可启动计费 T4-T11
**backlog 债务批次**: T17/T18/T19 互不依赖可并行；T20a 决策 → T20b 实现。与计费正交，可任选时机插入

---

## 各任务实施要点

### T4 — quota ↔ analytics 贯通 `[核心风险点]`

**当前状态**（v1.2.1 起始时审计结果）:
- ✅ quota-service 通过 `tokenLookup.getUsageSummary` **读取** analytics 累计用量做配额检查
- ✅ `bootstrap.ts` 在 claudeAdapter.onTaskComplete 回调里调 `tokenUsageService.recordUsage` 与 `billingService.recordEvent`
- ❌ **没有 quota 扣减（consume/deduct/reserve）的写路径** —— 当前是"读时检查"模式
- ❌ **没有日终对账任务**

**待确认点**:
- quota 扣减改同步还是异步？
- 是否需要预占机制（task 开始时预占 → 完成时实扣 / 失败时释放）？

### T10 — billing 写侧补全

**当前状态**: `billing-service.ts` 头注释明确写"不实现（本期骨架）：账单生成、充值/退款、对账任务"。

**需要实现**:
- `consume(tenantId, type, amount, metadata)` — 消费扣减（与 recordEvent 区别：consume 触发 quota 联动）
- `reserve(tenantId, amount)` / `release(reservationId)` — 预占/释放（用于 task 开始时锁定预算）
- `reconcile(tenantId, period)` — 对账（与 analytics 累计核对差异）

---

## 对后续版本的约束

1. **商业模式数据流不可绕开 analytics**: 计费相关事件必须落 analytics，不可只写 quota。
2. **billing 写侧必须有对账**: 不允许只累加 event 不核对，否则长期漂移不可发现。

## 遗留（版本完成后补）

- **D10 浏览器实测待用户执行**(P1):T27 已完成管理后台新建Agent→对话接线(后端 `instantiateExistingDefinition` + control route + `AgentCreateFlow` 创建后跳对话),后端 instanceId 链路 + route 注册(401)已验证,但前端 `openclawStore.activeInstanceId` 是否真设非 null + persona 真注入 + 真响应待浏览器 Console 实测。见 `docs/architecture/production-e2e-verification-checklist.md` 验证项 4
- **T18b 选项A E2E(T43 深查结论)**(P0→记遗留):路径 A 用 glm-4-flash **架构不可行**(claude-agent-sdk 强制暴露 19 内置工具 schema,glm 决策失焦不调业务工具,worker 代码不可绕)。**已修 3 项基础设施缺陷并提交**(LiteLLM 路由墙 config/worker 镜像重建含 T29/runner --add-host,对路径 B 无害)。**剩 sessionId resume bug**(adapter 传历史 sessionId 给 --rm worker resume 必 exit 1,待修)+ 真 E2E 需强模型(glm-4-plus/真 Anthropic)。投产走路径 B(T42 通过)。详见 `docs/architecture/private-deployment-paths.md` T43 实测结论
- **sessionId resume bug**(P2,新遗留,T43 发现):adapter 从 instance session store 取历史 sessionId 传 worker,worker 容器 `--rm` 无状态,`options.resume=sessionId` 让 Claude Code exit 1。修法:adapter 不传无效 sessionId 给 --rm worker(或 worker.ts resume 失败容错降级新会话)
- **OpenHands/多 Agent 框架适配**(P?,新规划,下一版本):现有 `IAgentRuntimeAdapter` 抽象已支持灵活适配任意 Agent 框架(registry 按 framework 路由,加 adapter 即插即用)。需新增 `OpenHandsAdapter implements IAgentRuntimeAdapter`(framework='openhands',工具收口走 registry.invoke)。前置:OpenHands 用什么模型待定(glm 同样可能遇大工具集问题)。注:OpenHands 是 Agent 平台非 SDK 替换依赖,server 侧零 SDK 依赖已实现(依赖隔离在 worker 容器)
- **本会话提交状态**(2026-06-26 第二轮,已 push):12 commit 基于 336af3e 已推 origin main(336af3e..87ec7a6)。本会话 4 commit:e671e85(fix路径A基础设施)+d74300c(docs T43结论)+2099d96(refactor T45拆分)+87ec7a6(docs T45完成)。前 8 commit 为 T38-T42+可观测性修复(已在上轮 push 前落本地)。`gh` 未装,PR 待用户(web 端创建)
- **实测环境运行中**(见 memory `grey-zone-runtime-env`):server(3002)+LiteLLM(4000)+mock marketplace(4080)+postgres(5435)在跑;.env 有 MARKETPLACE_API_URL/CLAUDE_INTERNAL_TOOL_SECRET/LITELLM_API_KEY(不进 git);enforce off;DB 有实测残留数据(上线前清理)
- **上线剩余阻断**(上线目标,2026-06-26 更新):~~T30~~+~~T38~~(done)+~~T43~~(done代码侧,3项基础设施修复已提交;真E2E需强模型记遗留,走路径B上线不阻断) → T9/D10 浏览器实测 → **T44** 数据清理+§14门禁归档 → 上线。计费 v1.2.2 T4-T11 排后(免费先上线)。**告警已就绪(T31)**
- ~~**dispatch 任务态/结论不可观测**(P2,T42 发现)~~(✅已修 2026-06-26,commit 1c26fd0):补 AdapterRegistry.getTaskStatus(遍历 adapter 跳过 not-found,单 adapter 抛错不阻断)+ harness 转发 + bootstrap GET /agent/status/:id。实测 dispatch tloop_→status 返回 completed+conclusion+toolCallsLog+turns,调用方可观测任务全生命周期。注:内存态,server 重启/多实例不持久(conclusion 落库留后续)
- **agent-executor.ts 824 行**(技术债,§14-10):超 800 评估线(未超 1000 红线),拆分待评估。§14 检测发现
- **dispatch conclusion 内存态**(技术债):/agent/status 端点(1c26fd0)查 adapter 内存,server 重启/多实例不持久,conclusion 落库留后续版本
- **§14-9 type-only 假环**(技术债,T44 检测):madge 报 adapter-registry↔agent-runtime-adapter / recommendation-engine↔recommendation-prompt 2 循环,经判定均为单向值 import + 反向 import type(编译擦除,运行时单向无环,tsc strict 通过),非真循环。建议下版本清理类型归属(类型挪独立文件打破静态环)
- **§14-6 memory/MEMORY.md 索引偏差**(技术债,T44 检测):CLAUDE.md §6.1 索引指向 `memory/MEMORY.md` 但项目内无此目录(记忆在全局 `~/.claude/projects/.../memory/`),索引与实际不符
- **budget-guard.ts 预留未用**(技术债,T44 检测,用户决策保留):harness/budget-guard.ts 零引用无 test,注释明确"供未来 Harness 统一预算分配/多 adapter 共享预算池",有意预留。保留记债,下版本决策接线或删
- **T17/T14 接线 bug 修复后实测**(P2,T44 修复):4 Section(Credential/FeatureFlag/RuntimeTemplates/ToolApprovals)已接入 AdminShell 投产管控组+系统组(import+NavItem+SwitchedSection 4case+adminStore HASHABLE 补3),三重门禁全绿。**admin 后台 4 功能可见性待浏览器实测**(代码侧已生效,实测为用户侧待办)
- **sessionId resume bug**(P2,T43遗留,路径A未启用不修):adapter(claude-agent-sdk-adapter.ts:130)传历史 sessionId 给 `--rm` worker,worker 无状态 resume 必 exit1。**路径 A(claude-agent-sdk)当前未启用**(T43 glm 在 SDK 19工具噪声下失焦,投产走路径B),此 bug 不触发。修法已明确:路径A启用前在 adapter 端不传无效 sessionId 给 --rm worker(改 adapter:157 sessionId 传 undefined,会话连续性靠 prompt history 维持)。当前盲改无法 E2E 验证,标注待路径A启用时修
- **§14-9 type-only 假环**(记录项,§14.2 不阻断):madge 报 adapter-registry↔agent-runtime-adapter / recommendation-engine↔recommendation-prompt 2 循环,经判定均为单向值 import + 反向 import type(编译擦除,运行时单向无环,tsc strict 通过)。§14.2 记录项不强行重构(避免 scope creep),下版本清理类型归属(类型挪独立文件打破静态环)
- **McpOpenApiFlow/McpGatewayFlow 假向导**(P2,用户决策保留作升级项):前端 setTimeout 假表演(MOCK_ENDPOINTS/MOCK_ROUTES 假"发现"),已加文件头标注"未接真实后端—升级项"。待真实 MCP 创建链路就绪后改造(参照 AppCreateFlow T52 真实 dispatch 模式)。详见 `docs/architecture/production-capability-boundary.md`
- **应用预览 MVP(技术验证通过,前端 iframe 已接)**(P1,T54→T56):后端 POST /agent/sandbox/:instanceId/preview 已实现(sandbox内npm install+vite dev background+端口转发URL),实测返回可访问URL。~~遗留前端 iframe 接线~~(T56 已接,startPreview→iframe 展示,处理 npm install 150s 超时+loading+error);~~sandbox 持久化硬伤~~(T55 已解决)
- **sandbox 持久化**(P0,投产阻断,T54 暴露→**T55 已解决**):OpenSandboxExecutor 的 sandbox 缓存是进程内(Map),server 重启后丢失 sandboxId。**已修复(T55)**:建 sandbox_sessions 表(caller_id↔sandbox_id 映射)+ migration 注册 + sandbox-session-repository(findSandboxSession/upsertSandboxSession/markSandboxEvicted/touchSandboxSession)。OpenSandboxExecutor.getOrCreateSandbox 改三段式:①进程缓存②DB 查 sandboxId → Sandbox.connect 重连已存在 sandbox③无记录则新建+落 DB;重连失败(sandbox TTL 过期)降级新建。touch 更新 DB last_used_at;evict 标记 DB evicted。**端到端实测验证通过**:创建应用 src/App.tsx → DB 落 sandboxId 映射 → 重启 HMR server(进程缓存清空)→ 读取仍返回真实文件内容(`Persist Survives Restart`)+ 文件树仍列 App.tsx。用户创建的应用**不再因 server 重启丢失**。门禁 server tsc 0+1678 passed+eslint 0

### 系统真实可用性差距(A/B/C 架构方向,待用户决策,非代码补丁可解)

> 2026-06-26 用户实测四维审计发现:门禁全绿但系统"执行/生成/统计能力缺失或假表演"。T48 修了统计(tool-loop 补写入),其余三类是架构决策,非 bug 修复:

- **A. 创建向导假表演**(用户决策:保留作升级项):`studio/create/` 下 AppCreateFlow(MOCK_TERMINAL 假"生成应用"动画+假代码模板)/McpOpenApiFlow(MOCK_ENDPOINTS+MOCK_SWAGGER 假发现)/McpGatewayFlow(MOCK_ROUTES 假路由)是纯前端 setTimeout 表演,无后端/LLM/沙箱。**保留待真实 coding agent 就绪后升级为真**(用户决策)。不删不改,但已标注避免误导
- **B. 对话无多轮+无真实生成能力**(架构方向,待决策):①openclaw chat.ts 是单轮 chatCompletion,无 agent loop ②tool-loop 有 while 多轮但工具是 DB business 工具(无代码生成/文件系统/shell)③claude-agent-sdk worker(真 coding agent+沙箱)T43 判定 glm 不可行搁置。**核心矛盾**:T32"换厂商不被绑定"决策牺牲了 coding agent 能力,退换成"能调 business 工具的对话+假表演向导"。三条路:(A)启用 claude-agent-sdk worker 需强模型(绑 Anthropic 协议,违背 T32)/ (B)给 tool-loop 加 coding 类工具+自建容器沙箱(不绑 Anthropic 但沙箱是基础设施,工作量大)/ (C)OpenHands adapter(开源 Agent 平台自带沙箱+coding,server 零 SDK 依赖,但模型待定且 glm 可能同样遇大工具集问题)。**此决策阻断"真实生成应用"能力,需用户拍方向后开新版本会话**
- **C. openclaw chat 路径用量未入账**(归 A/B 决策):chat.ts 单轮 LLM 调用不写统计(recordUsage 只挂 dispatch 路径)。chat 是否要 agent 能力+用量归户(instance/tenant 归属设计)待 A/B 方向定后统一处置
