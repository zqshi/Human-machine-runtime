# 新会话接手待办（2026-06-29 v2.1 E11 编排子系统完成）

> 读 CLAUDE.md §10 上下文加载协议 + 本文件 + `v2.1-current.md` + memory `v2.1-eaos-domain-phase-a.md` 继续。
> 上一会话：完成 v2.1 Phase C E11（cockpit 编排子系统 domain 建模），commit 65bbdb1 已 push origin/main，门禁全绿。

## 一、当前版本状态

**v2.1 进行中**（2026-06-29 激活，v2.0 已归档 snapshot）。主题：cockpit 五子系统从 EAV 贫血模型建为 domain 实体 + dispatch trace→涌现信号数据回流。

任务进度（见 `v2.1-current.md` 任务表）：

| Phase | 任务 | 状态 |
|---|---|---|
| A 感知 | E1-E6（骨架/signals 表/domain/service/route/trace 提取/correction seams） | done push 52d58b1 |
| B 战略解码 | E7-E9（objectives 表/domain/service/route） | done push 80be0a9 |
| C 判断 | E10（decisions+judgment_records/domain/service/route） | done push 65bbdb1 |
| C 编排 | E11（orchestration_chains/escalations/agents/domain/service/route） | done push 65bbdb1 |
| C 评估 | **E12（evaluation_metrics/scorecards/domain/service/route）** | pending ← 下一会话 |
| D 收尾 | E13（前端诚实化+D17 死代码+bootstrap 装配全量+文档状态+§14 门禁） | pending |

Phase A/B/C(E10/E11) 全部已 push origin/main。**E12 依赖 E1（已 done），无阻塞，可直接开始。**

## 二、下一会话做什么

### 推荐：E12 Evaluation 子系统（与 E10/E11 同构，风险最低）

下沉对象：`server/src/routes/cockpit/evaluation.ts`（108 行 EAV）+ 现有 `evaluation.test.ts`（124 行）。

**接手要点（详细版见 memory `v2.1-eaos-domain-phase-a.md` 末尾"新会话接手 E12 要点"）**：

1. **先审前端契约**：读 `client-suite/apps/web/src/infrastructure/api/cockpitApiAdapter.ts` 确认有无 evaluation DTO。**E11 教训：cockpitApiAdapter 全文无 orchestration DTO**——evaluation 大概率同样无 DTO，对标源应退化为**后端 evaluation.ts 现有 route 字段**（守 §6.3，不建前端设计态超前实体）。
2. **复用 E10/E11 模式**（已 4 次验证）：实体表(pgTable+index)+migration(幂等 CREATE/INDEX/INSERT EAV 迁移)+domain(private constructor+create/fromProps/rehydrate+状态机)+repository(InferSelectModel+mapRow+filter 下推 DB+listPaged)+service(注入 repo+eventBus)+route 薄层(serialize Date→ms)+bootstrap 内联装配。
3. E12 涉及 dual-track（human/agent 指标）+ scorecard 计算——这部分 `evaluation.ts` 已接真 LLM（C20 done），domain 建模只做实体化，**不动 LLM 接入逻辑**（防 scope creep）。
4. 评估前端 `AgentOrchestrationService.avgLatencyMs` 是 `Math.random()` 假数据——这是 E13 顺手项，E12 不碰。

### 或：E13 收尾（所有子系统实体化后做）
前端诚实化 + D17 死代码清理 + bootstrap 全量 service 注入核查 + 文档 02 [PLANNED]→[IMPLEMENTED] + §14 版本归档门禁。

## 三、关键纪律速览

### 门禁（提交前全过，husky pre-commit 自动跑 `set -e` 任一失败 abort，慢 ~60s）
```bash
cd server && npx tsc --noEmit && npx vitest run && npx eslint src/
# migration 实跑（§7.2.1#3，tsc/vitest 测不出缺表）
cd server && npm run db:migrate
```
- vitest **必须在 server 目录跑**（根目录用 monorepo v4.1.9 致 `describe is not defined` 假失败）。
- E11 后基线：tsc0 / vitest 2017 pass|3 skip / eslint0。

### 提交纪律（memory 教训）
- subject 小写中文开头（`feat(v2.1): cockpit 评估子系统...`），body **每行 <100 字符**（body-max-line-length，中英混合长行易超）。
- lint-staged prettier 残留（stash 恢复失败留工作区）：**重 git add 再 amend，勿 --no-verify**；push 前确认 `git status` 空。
- push 前确认工作区干净。

### 服务状态（实测用，本次未起）
postgres 5435 / LiteLLM 4000 / server 3002（tsx watch 热重载，改源码自动重载）。cockpit authMiddleware 需 token，端点实测待 server 起后 curl。

## 四、E11 核心审查判断（适用 E12，勿重蹈）

**E11 ≠ E10 的关键审查**：前端 `cockpitApiAdapter` 全文**无 orchestration DTO**（decisions 有 `toDecisionRequest` 守卫，orchestration 没有），前端 `EscalationChain`/`CollaborationChain` 字段不对等是设计态超前模型。对标源退化为**后端 route 现有字段**（守 Phase B/E10 教训 §6.3）。

E12 落地前必须同样审查 evaluation 的前端契约源——若 cockpitApiAdapter 无 evaluation DTO，对标后端 `evaluation.ts` route 字段，诚实分层（有固定契约的强类型列 + 无契约的 metadata jsonb 透传，不强加超前列）。

## 五、遗留约束（记入 v2.1-current.md + memory）

1. **破贫血进展**：cockpit 五子系统已实体化 4 个（signal/objective/decision/orchestration），剩 evaluation（E12）。EAV `cockpitEntities` 保留给非五子系统（notification 等），不 drop。
2. advance 真调度接 `/agent/dispatch` 留 [PLANNED]（v2.1 版本目标"编排接真路由"整体目标，E11 只打地基）。
3. orchestration 三端点前端不消费（孤儿），实体化破贫血 + 留真调度接口。
4. bootstrap ~822 行，逼近 1000 再抽 cockpit-bundle（同 signal/objective/decision 一致内联策略）。
5. 已有（需外部条件/决策，非 v2.1 范围）：计费 D15（需商业化时机）、运行时实测（Matrix E2E + cockpit 浏览器）、CubeSandbox KVM（D14）。
